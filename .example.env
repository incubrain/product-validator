# {CONFIG}: copy contents into a new .env and update with your values

# ANALYTICS 
# Pre configured using umami, free and have everything you need
# To change I would suggest using supported nuxt/scripts providers https://scripts.nuxt.com/scripts
# Then you will need to update the code here: app/middleware/
NUXT_PUBLIC_SCRIPTS_UMAMI_ANALYTICS_SCRIPT_INPUT_SRC="https://your-umami-instance.com/script.js"
NUXT_PUBLIC_SCRIPTS_UMAMI_ANALYTICS_WEBSITE_ID="website_id"
# Footer link to your analytics for quick access
NUXT_PUBLIC_ANALYTICS_LINK="share_link_from_analytics_provider"

# CONFIG
NUXT_PUBLIC_CONFIG_SOURCE="root"
NUXT_PUBLIC_DEBUG="false"

# HOSTING
PROVIDER="node_server" # Default - choose provider https://nuxt.com/deploy
PORT=3000
HOST=0.0.0.0

# ============================================
# WEBHOOK CONFIGURATION (Required)
# ============================================
# Server-only config - NOT exposed to client for security
# Choose ONE platform for lead notifications:
#
# TELEGRAM:
#   1. Create bot via @BotFather
#   2. Get chat ID (message your bot, visit https://api.telegram.org/bot<TOKEN>/getUpdates)
#   3. Format: https://api.telegram.org/bot<TOKEN>/sendMessage?chat_id=<CHAT_ID>
#
# SLACK:
#   1. Visit https://api.slack.com/messaging/webhooks
#   2. Create app → Incoming Webhooks → Add to channel
#   3. Copy webhook URL
#
# DISCORD:
#   1. Edit channel → Integrations → Webhooks
#   2. Create webhook → Copy URL
#
NUXT_WEBHOOK_URL=

# STUDIO SETUP - https://content.nuxt.com/docs/studio/setup
STUDIO_GITHUB_CLIENT_ID="your_github_client_id"
STUDIO_GITHUB_CLIENT_SECRET="your_github_client_secret"
STUDIO_GITHUB_BRANCH_NAME="develop || staging || main"

#############################################
# Email Configuration
#############################################

# Production SMTP (leave empty for dev - uses Ethereal)
EMAIL_ENABLED="false"
EMAIL_FROM="Your App <noreply@example.com>"
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# Ethereal Email (Dev - Optional)
# If you want persistent test account instead of generated one:
# ETHEREAL_USERNAME=
# ETHEREAL_PASSWORD=
# Dashboard: https://ethereal.email/login

