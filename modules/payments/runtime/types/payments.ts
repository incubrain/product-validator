// Re-export all types from module root to eliminate duplication
export type {
  // Provider abstraction
  PaymentProvider,
  ProviderRegistry,
  CheckoutOptions,
  
  // Product & config
  PaymentProduct,
  PaymentsModuleOptions,
  
  // Events
  PaymentEvent,
} from '../../types'

